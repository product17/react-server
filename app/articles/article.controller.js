'use strict';

var articleModel = require('./article.model');

exports.getArticleById = function (id) {
  return {
    _id: 'f43twafs',
    title: 'The prim and propper',
    body: '<p>wat wat, are we do do with alll this junk?</p>',
  }
}
